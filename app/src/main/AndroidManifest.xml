<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.baeumli.locus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.main.MainActivity">

        </activity>
        <activity
            android:name=".activities.main.SecondActivity"
            android:label="Get Second Activity"
            android:theme="@style/AppTheme" />
        <activity android:name=".activities.main.SplashActivity" android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.auth.LoginActivity">

        </activity>
        <activity android:name=".activities.auth.RegisterActivity">

        </activity>
    </application>

</manifest>